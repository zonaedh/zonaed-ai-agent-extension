{
  "name": "zonaed-ai-agent-extension",
  "version": "0.4.0",
  "private": true,
  "description": "Zonaed AI companion Chrome extension (WhatsApp CRM, Ghostwriter, Autofill, Page Watcher, OCR, Structured Scraping, YouTube Repurposing).",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src scripts --max-warnings=0",
    "verify": "npm run typecheck && npm run lint && npm run test && npm run build",
    "test": "tsx tests/pure.test.mts && tsx tests/crm.test.mts && tsx tests/edit-autofill.test.mts"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "rimraf": "^6.0.0",
    "tsx": "^4.23.13",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0"
  }
}
